style=kr #broken brackets by default, so we can define where to attach

indent=spaces=4 #indent using 4 spaces

add-brackets #add brackets to one-line statements

close-templates # vector<vector<int>> instead of vector< vector< int> >

pad-oper #pad around operators

indent-col1-comments #indent comments which start on the first line

indent-namespaces #indent namespaces

suffix=none #do not retain backups
